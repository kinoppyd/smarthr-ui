import * as React from 'react';
interface Props {
    children: React.ReactNode;
    bordered?: boolean;
    className?: string;
}
export declare const TabBar: (props: Props) => JSX.Element;
export {};
